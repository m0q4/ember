// URL del backend Ember API.
// Per lo sviluppo locale usa http://localhost:3000.
// Se il nome "ember-backend" e' libero su Render, l'URL di produzione sara'
// esattamente questo; se Render lo rinomina (nome gia' in uso da qualcun
// altro), aggiorna questa riga con l'URL reale mostrato nella dashboard di
// Render e fai un redeploy del sito statico.
window.EMBER_CONFIG = {
  BACKEND_URL: 'https://ember-backend.onrender.com',
};

